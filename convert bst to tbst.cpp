#include<bits/stdc++.h>
using namespace std;

struct Node{
    int key;
    Node *left , *right;
    // used to indicate if the right pointer to the ndoe is a 
    // normal pointer or the pointer to an inorder successor 
    bool isThreaded;
}

int main(){


}